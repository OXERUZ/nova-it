"use client";
import { ArrowRight, LockKeyhole, Mail, UserRound } from "lucide-react";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
import { createClient } from "@/lib/supabase";
export default function SignupPage(){const router=useRouter();const supabase=createClient();const [name,setName]=useState("");const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [loading,setLoading]=useState(false);const [error,setError]=useState("");const [done,setDone]=useState(false);async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError("");const {data,error}=await supabase.auth.signUp({email,password,options:{data:{full_name:name.trim()}}});if(error){setError(error.message);setLoading(false);return}if(data.session){router.replace("/dashboard")}else{setDone(true);setLoading(false)}}return <main className="auth-page"><div className="auth-orb auth-orb-a"/><div className="auth-shell admin-login"><a href="/" className="logo auth-logo">NOVA <span>ACADEMY</span></a><form className="auth-card" onSubmit={submit}><div className="auth-card-icon"><UserRound/></div><div className="eyebrow">CREATE ACCOUNT</div><h2>O‘quvchi hisobini yaratish</h2><p className="muted">Nova Academy platformasiga yangi o‘quvchi sifatida qo‘shiling.</p>{done?<><div className="reward-banner" style={{marginTop:22}}><Mail size={20}/><div><strong>Emailni tasdiqlang</strong><span>Email manzilingizga tasdiqlash havolasi yuborildi. Tasdiqlagach platformaga kiring.</span></div></div><a className="btn primary" style={{width:"100%",marginTop:18}} href="/login">Kirish <ArrowRight size={16}/></a></>:<><div className="field"><label>To‘liq ism</label><div className="input-icon"><UserRound size={16}/><input value={name} onChange={e=>setName(e.target.value)} placeholder="Ism Familiya" required minLength={2}/></div></div><div className="field" style={{marginTop:14}}><label>Email</label><div className="input-icon"><Mail size={16}/><input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" required/></div></div><div className="field" style={{marginTop:14}}><label>Parol</label><div className="input-icon"><LockKeyhole size={16}/><input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Kamida 6 belgi" required minLength={6}/></div></div>{error&&<p className="form-error">{error}</p>}<button className="btn primary btn-lg" style={{width:"100%",marginTop:18}} disabled={loading}>{loading?"Hisob yaratilmoqda...":"Hisob yaratish"}<ArrowRight size={17}/></button></>}<a className="back-link" href="/login">Mavjud hisobingiz bormi? Kirish</a></form></div></main>}
